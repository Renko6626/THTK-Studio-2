# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ghidra projects / disasm artifacts (binary, large, rebuildable — never commit)
*.gpr
*.rep
*_ghidra/
ghidra_projects/
# game binaries / assets (copyright — never commit)
*.exe
*.dat

# IDE 每次启动重写的 MCP 接线文件，含轮换的会话 Bearer token —— 不要提交
.mcp.json

# python cache
__pycache__/
*.pyc
# 旧 PyQt 版本 THTK-Studio 的本地参考拷贝(克隆到 /tmp,本来就在仓库外,这条仅作说明)
/tmp/thtk-old-ref/
